/// Firestore Seeder placeholder.
class FirestoreSeeder {
  /// No automatic seeding — all data in Firestore comes purely from actual database documents.
  static Future<void> seedIfEmpty() async {
    // Intentionally empty. No hardcoded or dummy data seeded to Firestore.
  }
}
